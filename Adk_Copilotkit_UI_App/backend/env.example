# Backend env – copy to .env and set values (.env is gitignored)
# Reference copilot-adk-app uses 8000 + 3000; this app uses 8001 + 3001 to avoid conflict.

# Required
GOOGLE_API_KEY=your_google_api_key_here
DATABASE_URL=postgresql+asyncpg://adk_user:adk_password@localhost:5433/adk_db_new

# Host and port (8001 so reference can keep using 8000; checked before start)
HOST=0.0.0.0
PORT=8001

# Model and CORS (frontend on 3001)
GEMINI_MODEL=gemini-2.0-flash
CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# Optional (defaults in config.py)
# APP_NAME=adk_copilotkit_app
# SESSION_TIMEOUT_SECONDS=604800
