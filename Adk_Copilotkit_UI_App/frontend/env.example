# Frontend env – copy to .env.local (.env.local is gitignored)
# Reference copilot-adk-app uses 8000; this app backend defaults to 8001. Frontend runs on 3001.

# Required (backend URL; this app backend default port 8001)
NEXT_PUBLIC_API_URL=http://localhost:8001
